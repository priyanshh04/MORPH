{
  "name": "morph",
  "version": "1.0.0",
  "private": true,
  "description": "MORPH - Multiformat Output & Representation Processing Hub for SIH26154",
  "type": "module",
  "scripts": {
    "start": "node --import ./packages/ai/length-control.js --env-file-if-exists=.env apps/api/server.js",
    "dev": "node --watch --import ./packages/ai/length-control.js --env-file-if-exists=.env apps/api/server.js",
    "test": "node tests/run-tests.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@neon/config": "^1.3.0",
    "@neon/env": "^1.2.1",
    "mammoth": "^1.12.2",
    "openai": "^7.10.0",
    "pdf-parse": "^2.4.5",
    "pg": "^8.23.0"
  }
}
